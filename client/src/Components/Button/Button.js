import React from 'react'; 

// Initially created just to test if gulp is working
const button = () => {
    return(
        <div>
            <button className="button">Gulp Test</button>
        </div>
    );
}

export default button; 