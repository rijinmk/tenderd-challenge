import React from 'react'; 

// Initially created just to test if gulp is working
const inputType = () => {
    return(
        <div>
            <input type="text"/>
        </div>
    );
}

export default inputType; 